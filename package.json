{
  "name": "sancheck-attendance",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'build ok'",
    "dev": "node server.js",
    "dev:sandbox": "wrangler pages dev dist --d1=sancheck-production --local --ip 0.0.0.0 --port 3000",
    "cf-build": "vite build",
    "cf-deploy": "npm run cf-build && wrangler pages deploy"
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "express": "^4.22.1",
    "hono": "^4.12.11"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
